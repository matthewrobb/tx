# twisted-decompose

Internal sub-skill loaded by `/twisted-work`. Handles **arch_review** and **decompose** steps.

---

## Arch Review Step

<%~ extract("src/decompose/arch-review.ts", "executeArchReview") %>

---

## Decompose Step

<%~ extract("src/decompose/breakdown.ts", "readInputsForDecompose") %>

<%~ extract("src/decompose/breakdown.ts", "estimateComplexity") %>

<%~ extract("src/decompose/breakdown.ts", "decomposeIntoIssues") %>

<%~ extract("src/decompose/breakdown.ts", "writeDecomposeOutput") %>
